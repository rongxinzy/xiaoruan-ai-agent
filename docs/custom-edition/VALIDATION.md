# 定制版验证

基线：`4b09981f7f7c454664ce212f06311c7a54682596`。验证日期：2026-09-07。

- 17 个相关测试文件、282 项测试通过，覆盖模型注册、可用模型列表、任务模型代理、应用身份隔离及安装器引用等。
- `npm run lint`、渲染进程 TypeScript、主进程 TypeScript、`npm run build` 通过。
- Electron 使用临时数据目录启动，不修改系统登录项。生产构建能够进入首页、打开设置和关于页，页面无未捕获渲染错误。
- preload 的 `auth`、`modelPool`、`appUpdate` 均不存在。首次启动无产品登录按钮。
- React 与 React DOM 使用 Vite 的 `resolve.dedupe` 统一解析，避免首次启动的重复 React 实例错误。

## 政务版品牌与主题（2026-09-07）

- 产品名、系统应用名、安装器文案、首页/侧栏/关于页统一为晓软政务办公智能体，使用用户提供标志整理后的白底应用图。
- 12 个相关测试文件、208 项测试通过，包含本轮修改的所有测试文件及主题开关、模型路由边界回归。
- `npm run lint`、渲染进程与主进程 TypeScript、`npm run build` 通过；最终文案调整后再次执行渲染构建与 lint。
- 使用 Playwright 启动真实 Electron，生产渲染构建配合开发主进程；Browser 插件未提供。临时用户数据，不修改系统登录项。
- 验证启动欢迎页正常退出、首页输入区可见、关于页标志加载、浅/深色切换、主题保存后重新加载、减少动态效果模式及 1024×740 紧凑窗口（应用最小尺寸）。无未捕获渲染错误，原产品认证/免费模型/更新桥接仍不存在。
- 默认主题 `classic-light` 的强调色为 `oklch(0.48 0.18 25)`；深色 `classic-dark` 为 `oklch(0.74 0.13 25)`。主题生成器同步保留了原正文与危险状态颜色。
- 启动截图仅在测试进程延迟首次配置读取 2 秒以便捕获，生产代码不增加品牌停留时间。首次未配置模型时会弹出模型设置，关闭后验证首页。

截图：[启动](boot.png)、[首页](home.png)、[关于](about.png)、[深色关于](about-dark.png)、[政务红外观](theme-light.png)、[深色外观](theme-dark.png)、[紧凑窗口](home-dark-compact.png)。

未验证：真实付费 API 请求、外部渠道收发、Windows/Linux 安装包、代码签名与公证。独立副本尚未下载可选的渠道、记忆和 Python 运行时，启动日志会提示其缺失；这不影响本次验证的首页与设置流程。构建仍有上游大分块和第三方 `import.meta` 的打包警告。
